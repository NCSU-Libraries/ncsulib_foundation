// instantiate foundation
(function ($, Drupal, window, document, undefined) {
	$(document).foundation();

	// globally turn tabs into an accordion
	// $('.tabs').
})(jQuery, Drupal, this, this.document);
